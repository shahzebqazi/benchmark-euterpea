# Data

Generated benchmark output lives here.

- `runs/`: raw JSON outputs from local model runs.

Raw runs are ignored by git by default. Promote only curated fixtures or summaries when they are needed for reproducible tests, reports, or public documentation.
