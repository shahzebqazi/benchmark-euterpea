# Agent Docs

This area is for instructions that help agents work on the benchmark safely.

Keep root `AGENTS.md` concise. Put longer operating notes here when they are useful for:

- adding tasks;
- writing deterministic verifiers;
- running local models;
- preserving git hygiene;
- separating prompts, expected answers, raw outputs, and derived summaries.
